/** @jsx jsx */
import { jsx } from '@emotion/core';
import css from '@styled-system/css';

const Footer = () => (
  <footer
    css={css({
      minHeight: '120px',
    })}
  />
);

export default Footer;
